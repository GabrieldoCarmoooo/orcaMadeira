export const MAX_UPLOAD_SIZE = 10 * 1024 * 1024 // 10MB

export const ACCEPTED_TYPES = ['.csv', '.xlsx', '.xls'] as const

export const COLUNAS_OBRIGATORIAS = ['nome', 'unidade', 'preco_unitario'] as const
